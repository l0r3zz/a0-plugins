name: faiss_integrity_check
title: FAISS Integrity Check
description: Validates FAISS vector store integrity on agent startup. Auto-recreates a fresh empty index if files are missing or corrupted to prevent recall crashes.
version: 1.0.0
settings_sections: []
per_project_config: false
per_agent_config: false
